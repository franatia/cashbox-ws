export enum OrderType {
    RESERVE = "RESERVE",
    STANDARD = "STANDARD",
    DROPSHIPPING = "DROPSHIPPING"
}

export enum OrderStatus {
    CART = "CART",
    OPEN = "OPEN",
    CONFIRMED = "CONFIRMED",
    FULLFILLED = "FULLFILLED",
    CANCELLED = "CANCELLED"
}